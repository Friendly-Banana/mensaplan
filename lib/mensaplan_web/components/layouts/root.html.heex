<!DOCTYPE html>
<html lang="en" class="[scrollbar-gutter:stable]">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <meta name="csrf-token" content={get_csrf_token()} />
    <.live_title suffix=" · Phoenix Framework">
      <%= assigns[:page_title] || "Mensaplan" %>
    </.live_title>
    <link phx-track-static rel="stylesheet" href={~p"/assets/app.css"} />
    <script defer phx-track-static type="text/javascript" src={~p"/assets/app.js"}>
    </script>
  </head>
  <body class="bg-white antialiased">
    <.flash_group flash={@flash} />
    <.navbar user={Plug.Conn.get_session(@conn, :current_user)} />

    <%= @inner_content %>

    <footer>
      <div class="flex flex-col sm:flex-row items-center justify-evenly p-2 text-sm">
        <img class="h-4" src={~p"/images/logo.svg"} alt="Logo" />
        <span>
          ©<%= DateTime.utc_now().year %> Gabriel
        </span>
        <span>
          Version <%= Application.spec(:mensaplan, :vsn) %>
        </span>
      </div>
    </footer>
  </body>
</html>
